This addon adds an optional workspace interface for OCA DMS.

It provides a more familiar file and folder workspace with a folder tree,
content cards/list view, inspector pane, context menus, upload actions, archive
and restore flows, and responsive layout behavior.

The addon keeps the existing DMS models, storage, permissions, content routes,
and technical file/directory views available.
